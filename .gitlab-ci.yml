.base_ruff:
  interruptible: true
  image: ghcr.io/astral-sh/ruff:alpine
  before_script:
    - cd $CI_PROJECT_DIR
    - ruff --version


Ruff Check:
  extends: .base_ruff
  script:
    - ruff check --output-format=gitlab --output-file=code-quality-report.json
  artifacts:
    reports:
      codequality: $CI_PROJECT_DIR/code-quality-report.json


Ruff Format:
  extends: .base_ruff
  script:
    - ruff format --diff


flake8:
  image: python:3.14-alpine
  script:
    - pip install flake8-gl-codeclimate
    - flake8 --version
    - |
      flake8 --exit-zero --format gl-codeclimate \
             --output-file $CI_PROJECT_DIR/gl-code-quality-report.json \
             $CI_PROJECT_DIR
  artifacts:
    reports:
      codequality: $CI_PROJECT_DIR/gl-code-quality-report.json

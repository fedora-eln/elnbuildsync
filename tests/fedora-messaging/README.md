This directory contains the public certificate and private keys for the Fedora
public message broker. For production use, we have a separate certificate and
key that works with the private broker and maintains persistent queue
contents.

These certificates were sourced from
https://github.com/fedora-infra/fedora-messaging/tree/develop/configs
and must be kept in sync with that location.

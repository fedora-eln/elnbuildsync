Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}
{{- if .Values.sidecar_database }}

IMPORTANT — PostgreSQL sidecar is for TESTING only:
  The bundled Postgres container is NOT intended for production deployments.
  Use a managed or dedicated PostgreSQL service for production.

  This mode requires Kubernetes 1.31+ (native sidecar init containers).

  In your Git-backed configuration, set db.host to 127.0.0.1, db.port to 5432,
  db.user to {{ .Values.sidecar_database_postgres.user }}, and db.name to {{ .Values.sidecar_database_postgres.database }}.

{{- if .Values.sidecar_database_persistent }}
  Postgres data is stored on PersistentVolumeClaim {{ .Values.app_name }}-postgres-data.
  Ensure persistence.size and persistence.storageClassName in values match your cluster.
  If you upgraded from an older chart/image (pre–Postgres 18 layout), delete that PVC
  once so a fresh cluster can initialize, or migrate data per upstream Postgres docs.
{{- else }}
  Postgres uses emptyDir; all database data is discarded when the Pod is deleted.
{{- end }}
{{- end }}
